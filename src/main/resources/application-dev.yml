cors:
  allowed-origins: http://localhost:4200

client_id_keycloak: app_jm_perfumaria

spring:
  datasource:
    url: jdbc:h2:file:/data/demo
    driver-class-name: org.h2.Driver
    username: sa
    password: sa

  jpa:
    hibernate:
      ddl-auto:  update
    database-platform: org.hibernate.dialect.H2Dialect

  h2:
    console:
      enabled: true

  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: http://localhost:8080/realms/manager
          jwk-set-uri: ${spring.security.oauth2.resourceserver.jwt.issuer-uri}/protocol/openid-connect/certs

          client:
            registration:
              keycloak:
                client-id: ${client_id_keycloak}
                authorization-grant-type: authorization_code
                scope: openid
            provider:
              keycloak:
                issuer-uri: http://localhost:8080/realms/manager
                user-name-attribute: preferred_username
#    user:
#      name: admin
#      password: R6Fk9;Z>wvI33ysLse3]ZBLx;ZC/&n